# NPM configuration for Coderrr
# Ensures package is published correctly

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions (not ranges)
save-exact=true

# Public access (for scoped packages)
access=public
